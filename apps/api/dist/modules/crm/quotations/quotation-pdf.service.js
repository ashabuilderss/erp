"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var QuotationPdfService_1;
Object.defineProperty(exports, "__esModule", { value: true });
exports.QuotationPdfService = void 0;
const common_1 = require("@nestjs/common");
const pdf_lib_1 = require("pdf-lib");
let QuotationPdfService = QuotationPdfService_1 = class QuotationPdfService {
    logger = new common_1.Logger(QuotationPdfService_1.name);
    async generateWatermarkedPdf(quotation, downloadedByEmail) {
        const pdfDoc = await pdf_lib_1.PDFDocument.create();
        const page = pdfDoc.addPage([600, 800]);
        const { width, height } = page.getSize();
        page.drawText(`Quotation: ${quotation.referenceNumber}`, {
            x: 50,
            y: height - 50,
            size: 20,
        });
        page.drawText(`Company: ${quotation.companies.name}`, {
            x: 50,
            y: height - 80,
            size: 12,
        });
        page.drawText(`Total Amount: ${quotation.totalAmount.toString()}`, {
            x: 50,
            y: height - 110,
            size: 12,
        });
        page.drawText(`Status: ${quotation.status}`, {
            x: 50,
            y: height - 140,
            size: 12,
        });
        page.drawText(`Generated By: ${quotation.createdBy?.userId || 'System'}`, {
            x: 50,
            y: height - 170,
            size: 12,
        });
        const breakdown = typeof quotation.breakdown === 'object'
            ? JSON.stringify(quotation.breakdown)
            : '{}';
        page.drawText(`Breakdown: ${breakdown}`, {
            x: 50,
            y: height - 200,
            size: 10,
        });
        const watermarkText = `CONFIDENTIAL - Downloaded by: ${downloadedByEmail} at ${new Date().toISOString()}`;
        page.drawText(watermarkText, {
            x: 50,
            y: height / 2 - 100,
            size: 24,
            color: (0, pdf_lib_1.rgb)(0.75, 0.75, 0.75),
            rotate: (0, pdf_lib_1.degrees)(45),
            opacity: 0.3,
        });
        const pdfBytes = await pdfDoc.save();
        return Buffer.from(pdfBytes);
    }
};
exports.QuotationPdfService = QuotationPdfService;
exports.QuotationPdfService = QuotationPdfService = QuotationPdfService_1 = __decorate([
    (0, common_1.Injectable)()
], QuotationPdfService);
//# sourceMappingURL=quotation-pdf.service.js.map